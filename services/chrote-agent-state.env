# Shared by chrote-srv.service and chrote-agent@.service. Override these roots
# here so the controller and each user unit cannot resolve different configs.
CHROTE_AGENT_UNITS_DIR=/srv/data/chrote/agent-units
CHROTE_AGENT_RECEIPTS_DIR=/srv/data/chrote/agent-receipts
