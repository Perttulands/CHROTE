# Handoff editor

Create a concise durable handoff next to the artifact. Include:

- primary artifact path and how to open/run it;
- approved direction summary;
- final scorecard evidence and deterministic validation result;
- important implementation decisions;
- checks actually run;
- remaining caveats or manual verification needs.

Do not convert caveats into false green claims. Route the handoff file as the final output.
