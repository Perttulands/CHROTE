# Tailscale auth key - get from https://login.tailscale.com/admin/settings/keys
TS_AUTHKEY=tskey-auth-XXXXXXXXXXXXXXXX

# Filebrowser credentials (REQUIRED for first-time setup)
FB_ADMIN_USER=admin
FB_ADMIN_PASS=your-secure-password-here

# API Authentication (optional - set to enable bearer token auth)
# If set, all /api/* requests require: Authorization: Bearer <token>
API_AUTH_TOKEN=

# CORS allowed origins (optional - comma-separated list)
# If not set, allows all origins (dev mode)
# Example: CORS_ORIGINS=https://arena.example.com,https://localhost:5173
CORS_ORIGINS=

# Beads API allowed roots (optional - comma-separated paths)
# Restricts which directories the beads API can access
# Default: /code,/workspace
BEADS_ALLOWED_ROOTS=/code,/workspace
